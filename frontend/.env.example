# ========================================
# StyleStore frontend (.env)
# Vite only exposes variables prefixed with VITE_.
# Copy this value into the root .env / .env.local
# so the React app can reach the Express email API.
# ========================================
VITE_API_URL=http://localhost:3001